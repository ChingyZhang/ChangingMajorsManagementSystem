<script type="text/javascript">


    $(function () {
        //archbox 中的 select 事件
        bodyclick = document.getElementsByTagName('body').item(0);
        init('main');
        rSelects();
        bodyclick.onclick = function () {
            for (i = 0; i < selects.length; i++) {
                //alert(selects[i].name);
                document.getElementById('select_info_' + selects[i].name).className = 'tag_select';
                //                                $("#select_info_" + selects[i].name).attr("class", "tag_select");
                //                                $("#options_" + selects[i].name).css("display", "none");
                document.getElementById('boxul_' + selects[i].name).style.display = 'none';
                //			$('select_info_' + selects[i].name).className = 'tag_select';
                //			$('options_' + selects[i].name).style.display = 'none';
            }
        }
        //archbox 中的 select 事件
    });


    function aa() {
        $.ajax({
            type: 'POST',
            url: '/Major/AddMajor',
            data: {},
            success: function (data) {
                $("#pageAddMaj").html(data);
                $("#pageAddMaj").css("display", "block");
            }
        });
    }

    //    $(function () {
    //        easychange.call(document.getElementById('year1'), function (value) {
    //            aa();
    //            $("#pageAddMaj").css("display", "block");
    //        });
    //    });
</script>
<div id="majorControl">
    <table cellpadding="5px" cellspacing="5px" border="0px">
        <tr>
            <td>
                请选择导入文件的为哪一年的数据:
            </td>
            <td>
                <div class="uboxstyle">
                    @{var year = ViewData["year"] as List<SelectListItem>;}
                    @Html.DropDownList("year1", year)
                </div>
            </td>
            <td>
                <div class="button">
                    <a href="#"><span onclick="aa();">确定</span></a>
                </div>
            </td>
        </tr>
    </table>
    <div id="pageAddMaj" style="display: none">
    </div>
</div>
